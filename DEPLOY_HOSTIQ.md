# Перенос проекта на HOSTiQ.ua

## ✅ Можно ли перенести?

**Да, можно!** HOSTiQ.ua поддерживает:
- ✅ Node.js приложения
- ✅ PostgreSQL базы данных
- ✅ Домены

## 📋 Требования к хостингу

Для вашего приложения нужен **VPS** или **выделенный сервер**, потому что:
- Нужен **Node.js** для backend сервера
- Нужна **PostgreSQL** база данных
- Нужна возможность запускать процесс постоянно (PM2 или systemd)

**Обычный виртуальный хостинг** может не подойти, так как там ограничена поддержка Node.js.

## 🚀 Варианты размещения на HOSTiQ.ua

### Вариант 1: VPS сервер (РЕКОМЕНДУЕТСЯ)

**Преимущества:**
- Полный контроль над сервером
- Можно установить Node.js, PostgreSQL, PM2
- Подходит для продакшена
- Выделенные ресурсы

**Необходимо:**
- VPS с Ubuntu/Debian/CentOS
- Минимум 1GB RAM (рекомендуется 2GB)
- Root доступ

### Вариант 2: Выделенный сервер

**Преимущества:**
- Максимальная производительность
- Полный контроль
- Подходит для больших проектов

**Недостатки:**
- Выше стоимость
- Нужны знания администрирования

### Вариант 3: Node.js хостинг (если доступен)

HOSTiQ.ua может предоставлять специальный Node.js хостинг через cPanel. Уточните в поддержке.

## 📝 Пошаговая инструкция переноса

### Шаг 1: Уточните у HOSTiQ.ua

Свяжитесь с поддержкой HOSTiQ.ua:
1. Подтвердите, что у вас есть **VPS** или **Node.js хостинг**
2. Узнайте, включена ли поддержка **PostgreSQL**
3. Спросите про **PM2** или другой процесс-менеджер
4. Узнайте, можно ли использовать **SSL сертификат** для HTTPS

### Шаг 2: Подготовка на VPS (если у вас VPS)

#### 2.1. Подключение к серверу

```bash
ssh root@ваш-ip-адрес
```

#### 2.2. Установка Node.js

```bash
# Обновляем систему
apt update && apt upgrade -y

# Устанавливаем Node.js 18+ (через NodeSource)
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt install -y nodejs

# Проверяем версию
node --version
npm --version
```

#### 2.3. Установка PostgreSQL

```bash
# Устанавливаем PostgreSQL
apt install -y postgresql postgresql-contrib

# Запускаем PostgreSQL
systemctl start postgresql
systemctl enable postgresql

# Создаем пользователя и базу данных
sudo -u postgres psql
```

В PostgreSQL консоли:
```sql
CREATE USER magnet_user WITH PASSWORD 'ваш-надежный-пароль';
CREATE DATABASE magnet_db OWNER magnet_user;
GRANT ALL PRIVILEGES ON DATABASE magnet_db TO magnet_user;
\q
```

#### 2.4. Установка PM2 (процесс-менеджер)

```bash
npm install -g pm2
```

#### 2.5. Настройка Git

```bash
apt install -y git
git clone https://github.com/ваш-username/magnet-app.git
cd magnet-app
```

### Шаг 3: Настройка приложения

#### 3.1. Установка зависимостей

```bash
cd /path/to/magnet-app/server
npm install

cd ../client
npm install
npm run build
```

#### 3.2. Создание файла .env

```bash
cd /path/to/magnet-app/server
nano .env
```

Добавьте:
```
DATABASE_URL=postgresql://magnet_user:ваш-пароль@localhost:5432/magnet_db
PORT=5000
NODE_ENV=production
```

#### 3.3. Настройка переменных окружения для фронтенда

В файле `client/.env.production`:
```
REACT_APP_API_URL=https://ваш-домен.com/api
```

Или настройте через веб-сервер (Nginx).

### Шаг 4: Настройка Nginx (веб-сервер)

#### 4.1. Установка Nginx

```bash
apt install -y nginx
```

#### 4.2. Создание конфигурации

```bash
nano /etc/nginx/sites-available/magnet-app
```

Добавьте:
```nginx
# Backend API
server {
    listen 80;
    server_name api.ваш-домен.com;

    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

# Frontend
server {
    listen 80;
    server_name ваш-домен.com www.ваш-домен.com;

    root /path/to/magnet-app/client/build;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Активируйте:
```bash
ln -s /etc/nginx/sites-available/magnet-app /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx
```

### Шаг 5: Запуск приложения

#### 5.1. Запуск через PM2

```bash
cd /path/to/magnet-app/server
pm2 start index.js --name magnet-app-backend
pm2 save
pm2 startup
```

#### 5.2. Проверка

```bash
pm2 status
pm2 logs magnet-app-backend
```

### Шаг 6: Настройка SSL (HTTPS)

#### 6.1. Установка Certbot

```bash
apt install -y certbot python3-certbot-nginx
certbot --nginx -d ваш-домен.com -d www.ваш-домен.com
```

#### 6.2. Автоматическое обновление

```bash
certbot renew --dry-run
```

### Шаг 7: Настройка домена в HOSTiQ.ua

1. Войдите в панель управления HOSTiQ.ua
2. Найдите ваш домен
3. Настройте DNS записи:
   - **A-запись**: `@` → IP адрес вашего VPS
   - **A-запись**: `www` → IP адрес вашего VPS
   - **A-запись**: `api` → IP адрес вашего VPS (если используете поддомен)

## 🔄 Альтернатива: Использование HOSTiQ.ua бесплатного переноса

HOSTiQ.ua предлагает **бесплатную услугу переноса**:

1. Заполните форму на: https://hostiq.ua/transfer-business/
2. Предоставьте:
   - Доступы к текущему хостингу (Render.com)
   - Информацию о проекте (Node.js + PostgreSQL)
   - Требования к серверу
3. Специалисты HOSTiQ.ua:
   - Подберут подходящий тариф
   - Выполнят перенос
   - Настроят все необходимое

## ⚠️ Важные моменты

1. **База данных**: Убедитесь, что PostgreSQL доступен на VPS или используйте внешний PostgreSQL
2. **Процесс-менеджер**: Используйте PM2 для автозапуска и перезапуска при сбоях
3. **Бэкапы**: Настройте регулярные бэкапы базы данных
4. **Мониторинг**: Настройте мониторинг логов через PM2 или другой инструмент
5. **Обновления**: Настройте автоматические обновления системы

## 📞 Контакты

- **Поддержка HOSTiQ.ua**: Свяжитесь через панель управления или по телефону
- **Техническая поддержка**: Уточните все детали перед переносом

## 🆚 Сравнение с Render.com

| Параметр | Render.com | HOSTiQ.ua VPS |
|----------|------------|---------------|
| **Стоимость** | Бесплатно (ограничения) | Платно (от ~$5/мес) |
| **Управление** | Автоматическое | Ручное |
| **Производительность** | Ограничена | Выделенная |
| **Надежность** | Высокая | Зависит от вас |
| **Поддержка** | Документация | Техподдержка |

## ✅ Рекомендация

**Используйте бесплатную услугу переноса HOSTiQ.ua** - это сэкономит время и нервы. Специалисты настроят все правильно.

Если хотите делать самостоятельно - следуйте инструкции выше.


