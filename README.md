# Magnet App - Система управления производственными заказами

Веб-приложение для управления производственными заказами с отслеживанием процессов выполнения.

## Возможности

- 🔐 Авторизация сотрудников
- 📋 Просмотр заказов в работе и выполненных заказов
- ⚙️ Управление процессами выполнения заказов
- ⏱️ Отслеживание времени начала и окончания процессов
- 👥 Поддержка одновременной работы нескольких сотрудников над одним заказом
- 🔧 Проверка занятости оборудования (тигели для высечки)
- ✅ Автоматическое завершение заказа после выполнения всех процессов

## Технологии

- **Frontend**: React
- **Backend**: Node.js + Express
- **База данных**: SQLite
- **Аутентификация**: JWT

## Требования

- Node.js версии 14 или выше
- npm (устанавливается вместе с Node.js)

## Установка и запуск

### 1. Установка зависимостей

```bash
npm run install-all
```

### 2. Запуск приложения

Запуск сервера и клиента одновременно:

```bash
npm run dev
```

Или отдельно:

```bash
# Backend (порт 5000)
npm run server

# Frontend (порт 3000)
npm run client
```

### 3. Доступ к приложению

Откройте браузер и перейдите по адресу: http://localhost:3000

## Тестовые данные

**Логин**: admin  
**Пароль**: admin123

Тестовый заказ #1825 уже создан в системе.

## Структура проекта

```
Magnet_app/
├── client/          # React frontend
│   ├── src/
│   │   ├── pages/   # Страницы приложения
│   │   ├── context/ # Контекст аутентификации
│   │   └── App.js   # Главный компонент
│   └── package.json
├── server/          # Node.js backend
│   ├── routes/      # API маршруты
│   ├── middleware/  # Middleware для аутентификации
│   ├── database.js  # Работа с базой данных
│   └── index.js     # Точка входа сервера
└── package.json
```

## Процессы производства

Система поддерживает следующие процессы:

1. Печать
2. Ламинация
3. Кашировка
4. Порезка магнита
5. Прикатка скотча
6. Плотерная порезка
7. Заливка смолы
8. Выкладка на стекла
9. Снятие со стекол
10. Упаковка
11. Высечка (с выбором тигеля)
12. Упаковка Флоу Пак
13. Упаковка в пакет с отрывной лентой
14. Выборка
15. Порезка Резак
16. Хоз работы

## API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход в систему
- `GET /api/auth/me` - Получить текущего пользователя

### Заказы
- `GET /api/orders` - Получить список заказов
- `GET /api/orders/:orderId` - Получить детали заказа
- `POST /api/orders/:orderId/complete` - Завершить заказ

### Процессы
- `POST /api/processes/:processId/start` - Начать выполнение процесса
- `POST /api/processes/:processId/complete` - Завершить выполнение процесса
- `GET /api/processes/:processId/executions` - Получить историю выполнения

## Особенности

- **Многопользовательская работа**: Несколько сотрудников могут одновременно работать над разными процессами одного заказа
- **Проверка занятости оборудования**: Для процесса "Высечка" система проверяет, не занят ли выбранный тигель другим сотрудником
- **Автоматическое обновление**: Страница деталей заказа обновляется каждые 5 секунд
- **Валидация**: Система не позволяет завершить заказ, пока не выполнены все процессы

