# Инструкция по деплою приложения

## ⚠️ Важно: InfinityFree не поддерживает Node.js

InfinityFree поддерживает только PHP и MySQL, поэтому для этого приложения нужен другой хостинг.

## 🚀 Рекомендуемые бесплатные варианты

### Вариант 1: Render.com (РЕКОМЕНДУЕТСЯ) ⭐

**Преимущества:**
- Полностью бесплатно
- Поддержка Node.js
- Автоматический деплой из GitHub
- Бесплатный SSL сертификат
- Поддержка SQLite

#### Шаги деплоя:

1. **Создайте аккаунт на Render.com**
   - Перейдите на https://render.com
   - Зарегистрируйтесь через GitHub

2. **Подготовьте проект для деплоя**

   Создайте файл `render.yaml` в корне проекта (я создам его ниже)

3. **Загрузите код на GitHub**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin https://github.com/ваш-username/magnet-app.git
   git push -u origin main
   ```

4. **Деплой на Render:**
   - Войдите в Render Dashboard
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Выберите репозиторий `magnet-app`
   - Настройки:
     - **Name**: magnet-app-backend
     - **Environment**: Node
     - **Build Command**: `cd server && npm install`
     - **Start Command**: `cd server && npm start`
     - **Root Directory**: server
   - Нажмите "Create Web Service"

5. **Деплой Frontend:**
   - Нажмите "New +" → "Static Site"
   - Подключите тот же репозиторий
   - Настройки:
     - **Name**: magnet-app-frontend
     - **Build Command**: `cd client && npm install && npm run build`
     - **Publish Directory**: client/build
   - В переменных окружения добавьте:
     ```
     REACT_APP_API_URL=https://ваш-backend-url.onrender.com/api
     ```

---

### Вариант 2: Railway.app

**Преимущества:**
- Очень простой деплой
- Поддержка Node.js
- $5 бесплатных кредитов в месяц

#### Шаги:

1. Зарегистрируйтесь на https://railway.app
2. Нажмите "New Project" → "Deploy from GitHub repo"
3. Выберите ваш репозиторий
4. Railway автоматически определит Node.js проект
5. Для frontend создайте отдельный сервис

---

### Вариант 3: Vercel (Frontend) + Render (Backend)

**Преимущества:**
- Vercel отлично подходит для React
- Быстрый CDN
- Render для backend

#### Шаги:

**Backend на Render:**
- Следуйте инструкциям из Варианта 1

**Frontend на Vercel:**
1. Зарегистрируйтесь на https://vercel.com
2. Подключите GitHub репозиторий
3. Настройки:
   - **Framework Preset**: Create React App
   - **Root Directory**: client
   - **Build Command**: `npm run build`
   - **Output Directory**: build
4. Добавьте переменную окружения:
   ```
   REACT_APP_API_URL=https://ваш-backend-url.onrender.com/api
   ```

---

## 📝 Файлы для деплоя

Я создам необходимые конфигурационные файлы ниже.

## 🔧 Настройка переменных окружения

После деплоя backend, получите его URL (например: `https://magnet-app-backend.onrender.com`)

Затем в настройках frontend добавьте:
```
REACT_APP_API_URL=https://magnet-app-backend.onrender.com/api
```

## ✅ Проверка работы

1. Откройте URL вашего frontend
2. Попробуйте войти (admin / admin123)
3. Если есть ошибки CORS, проверьте настройки CORS в backend

