# 🚀 Простая инструкция: Как задеплоить на Render.com

## ⚠️ Почему не InfinityFree?

InfinityFree поддерживает только PHP, а ваше приложение написано на Node.js. Поэтому используем Render.com - он бесплатный и поддерживает Node.js.

---

## 📋 Что нужно сделать (3 шага):

### 1️⃣ Загрузить код на GitHub

Это делается в **два этапа**: сначала на сайте GitHub, потом в терминале на Mac.

---

#### 📱 ЭТАП 1: Создать репозиторий на GitHub (в браузере)

1. Откройте браузер и зайдите на https://github.com
2. Войдите в аккаунт (или зарегистрируйтесь, если нет)
3. Нажмите кнопку **"+"** в правом верхнем углу
4. Выберите **"New repository"**
5. В поле "Repository name" введите: `magnet-app`
6. **НЕ ставьте галочки** на "Add a README file", ".gitignore", "license"
7. Нажмите зеленую кнопку **"Create repository"**
8. На следующей странице вы увидите URL вашего репозитория
9. **Скопируйте этот URL** (он будет выглядеть так: `https://github.com/ваш-username/magnet-app.git`)

---

#### 💻 ЭТАП 2: Загрузить код через терминал (на вашем Mac)

1. На вашем Mac откройте приложение **"Терминал"** (Terminal)
   - Нажмите Cmd + Пробел, введите "Terminal" и нажмите Enter
   
2. В терминале выполните команды **по порядку** (копируйте и вставляйте по одной):

```bash
# Шаг 1: Перейти в папку проекта
cd /Users/romanbilskiy/Documents/Magnet_app
```

```bash
# Шаг 2: Инициализировать Git
git init
```

```bash
# Шаг 3: Добавить все файлы
git add .
```

```bash
# Шаг 4: Создать первый коммит
git commit -m "First commit"
```

```bash
# Шаг 5: Подключить GitHub (замените URL на тот, что скопировали!)
git remote add origin https://github.com/ВАШ-USERNAME/magnet-app.git
```
⚠️ **Замените `ВАШ-USERNAME` на ваш реальный GitHub username!**

```bash
# Шаг 6: Установить ветку main
git branch -M main
```

```bash
# Шаг 7: Загрузить код на GitHub
git push -u origin main
```

---

**Если при `git push` просит пароль:**
- GitHub больше не принимает обычные пароли
- Нужно создать Personal Access Token:
  1. На GitHub: Settings → Developer settings → Personal access tokens → Tokens (classic)
  2. Generate new token (classic) → выберите `repo` → Generate
  3. Скопируйте токен (показывается только один раз!)
  4. Используйте этот токен вместо пароля при `git push`

---

### 2️⃣ Задеплоить Backend

1. Зайдите на https://render.com
2. Зарегистрируйтесь через GitHub
3. Нажмите "New +" → "Web Service"
4. Подключите ваш GitHub репозиторий `magnet-app`
5. Заполните:
   - **Name**: `magnet-backend`
   - **Root Directory**: `server`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
6. Нажмите "Create Web Service"
7. Дождитесь деплоя (2-3 минуты)
8. **Скопируйте URL** (например: `https://magnet-backend.onrender.com`)

---

### 3️⃣ Задеплоить Frontend

1. В Render нажмите "New +" → "Static Site"
2. Подключите тот же репозиторий `magnet-app`
3. Заполните:
   - **Name**: `magnet-frontend`
   - **Root Directory**: `client`
   - **Build Command**: `npm install && npm run build`
   - **Publish Directory**: `build`
4. В "Environment Variables" добавьте:
   - **Key**: `REACT_APP_API_URL`
   - **Value**: `https://magnet-backend.onrender.com/api`
   (Замените `magnet-backend` на имя вашего backend сервиса!)
5. Нажмите "Create Static Site"
6. Дождитесь деплоя

---

## ✅ Готово!

Откройте URL вашего фронтенда и войдите:
- Логин: `admin`
- Пароль: `admin123`

---

## ❓ Проблемы?

**Не работает?** 
- Проверьте, что `REACT_APP_API_URL` правильный (должен быть URL backend + `/api`)
- Убедитесь, что backend запустился (откройте его URL в браузере)

**Нужна помощь?** 
- Подробная инструкция в файле `DEPLOY_RENDER.md`

