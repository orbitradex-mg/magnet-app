# Пошаговая инструкция: Деплой на Render.com

## 🎯 Что нужно сделать

1. Загрузить код на GitHub
2. Задеплоить Backend на Render
3. Задеплоить Frontend на Render
4. Настроить переменные окружения

---

## Шаг 1: Загрузка на GitHub

### Если у вас еще нет Git репозитория:

```bash
# В корневой папке проекта
cd /Users/romanbilskiy/Documents/Magnet_app

# Инициализация Git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit - Magnet App"

# Создайте репозиторий на GitHub.com, затем:
git remote add origin https://github.com/ВАШ-USERNAME/magnet-app.git
git branch -M main
git push -u origin main
```

**Как создать репозиторий на GitHub:**
1. Зайдите на https://github.com
2. Нажмите "+" → "New repository"
3. Название: `magnet-app`
4. Выберите "Public" (или Private)
5. НЕ ставьте галочки (README, .gitignore, license)
6. Нажмите "Create repository"
7. Скопируйте URL и выполните команды выше

---

## Шаг 2: Деплой Backend на Render

1. **Зарегистрируйтесь на Render:**
   - Перейдите на https://render.com
   - Нажмите "Get Started for Free"
   - Войдите через GitHub (рекомендуется)

2. **Создайте Web Service:**
   - В Dashboard нажмите "New +" → "Web Service"
   - Выберите "Connect GitHub"
   - Разрешите доступ к репозиторию `magnet-app`
   - Выберите репозиторий `magnet-app`

3. **Настройте Backend:**
   - **Name**: `magnet-app-backend`
   - **Environment**: `Node`
   - **Region**: Выберите ближайший (например, Frankfurt)
   - **Branch**: `main`
   - **Root Directory**: `server`
   - **Build Command**: `npm install`
   - **Start Command**: `npm start`
   - **Plan**: `Free`

4. **Переменные окружения (Environment Variables):**
   - Нажмите "Advanced"
   - Добавьте переменные:
     ```
     NODE_ENV = production
     PORT = 10000
     ```

5. **Нажмите "Create Web Service"**

6. **Дождитесь деплоя** (2-3 минуты)

7. **Скопируйте URL** вашего сервиса (например: `https://magnet-app-backend.onrender.com`)

---

## Шаг 3: Деплой Frontend на Render

1. **Создайте Static Site:**
   - В Dashboard нажмите "New +" → "Static Site"
   - Выберите "Connect GitHub"
   - Выберите репозиторий `magnet-app`

2. **Настройте Frontend:**
   - **Name**: `magnet-app-frontend`
   - **Branch**: `main`
   - **Root Directory**: `client`
   - **Build Command**: `npm install && npm run build`
   - **Publish Directory**: `build`

3. **Переменные окружения:**
   - Добавьте переменную:
     ```
     REACT_APP_API_URL = https://magnet-app-backend.onrender.com/api
     ```
     ⚠️ **ВАЖНО:** Замените `magnet-app-backend` на реальное имя вашего backend сервиса!

4. **Нажмите "Create Static Site"**

5. **Дождитесь деплоя** (2-3 минуты)

6. **Скопируйте URL** вашего фронтенда (например: `https://magnet-app-frontend.onrender.com`)

---

## Шаг 4: Проверка работы

1. Откройте URL вашего фронтенда
2. Попробуйте войти:
   - Логин: `admin`
   - Пароль: `admin123`

3. Если видите ошибки в консоли браузера (F12), проверьте:
   - Правильно ли указан `REACT_APP_API_URL` в переменных окружения
   - Работает ли backend (откройте URL backend + `/api/auth/me`)

---

## 🔧 Решение проблем

### Проблема: CORS ошибки

**Решение:** Backend уже настроен на работу с любым доменом. Если проблемы остаются, проверьте переменную `FRONTEND_URL` в backend.

### Проблема: Frontend не подключается к Backend

**Решение:**
1. Проверьте, что `REACT_APP_API_URL` в frontend начинается с `https://` и заканчивается на `/api`
2. Убедитесь, что backend работает (откройте его URL в браузере)

### Проблема: База данных не работает

**Решение:** SQLite работает на Render, но данные могут теряться при перезапуске. Для продакшена лучше использовать PostgreSQL (Render предоставляет бесплатный PostgreSQL).

---

## 💡 Полезные ссылки

- Render Dashboard: https://dashboard.render.com
- Render Docs: https://render.com/docs
- Ваш Backend URL: https://ваш-backend-name.onrender.com
- Ваш Frontend URL: https://ваш-frontend-name.onrender.com

---

## 📝 Примечания

- **Бесплатный план Render** может "засыпать" после 15 минут неактивности. Первый запрос после этого может занять 30-60 секунд.
- Для продакшена рекомендуется использовать платный план или другой хостинг.
- База данных SQLite на бесплатном плане может терять данные при перезапуске.

---

## ✅ Готово!

После выполнения всех шагов ваше приложение будет доступно по адресу фронтенда!

